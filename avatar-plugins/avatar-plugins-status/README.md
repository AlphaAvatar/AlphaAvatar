# AlphaAvatar Status Plugin

Status event plugin for AlphaAvatar.

This plugin provides default implementations for:

- Status policy
- Status renderer
- Status sink

The core status protocol lives in `alphaavatar.agents.status`.
This plugin only provides replaceable runtime implementations.
